# 内容待办清单

> 盘点日期：2026-09-12（2026-09-13 更新：34 份教学 PDF 已上传）
> 用途：列出网站上**所有还没填内容的位置**，填完一项划掉一项。
> 所有行号都指 `docs/content.json`（除非另行注明）。

---

## 一、紧急：现在点开会出错的地方（剩 1 处）

| # | 位置 | 问题 | 怎么修 |
|---|---|---|---|
| ~~1~~ | ~~`diet-pdf-1`~~ | ✅ **已修复**：坏链接已随 33 份真实 PDF 上传一并清除 | — |
| 2 | `diet-vid-1` | 卡片写着"营养早餐怎么搭？"，作者是"**示例作者**"，链接是微信示例参数（`__biz=example`），点开是无效页面 | 换成真实视频链接，或清空 |

现在只剩营养饮食那条示例视频是"看起来有内容、点下去却失败"的，建议最先处理。

---

## 二、待补总量

| 类型 | 总位置数 | 已填 | 待补 |
|---|---|---|---|
| PDF 手册 | 80 | **34** | 46 |
| 视频 | 80 | 3（其中 1 条是假数据） | 77 |

> **注意**：每个模块上限是 10 条 PDF + 10 条视频，这是**上限不是目标**。每个模块填 1–2 条，页面就已经是"完整"的样子了，不必凑满。

---

## 三、逐模块明细

| 模块 | PDF 已填 | PDF 待补 | 视频已填 | 视频待补 |
|---|---|---|---|---|
| 营养饮食 `diet` | ✅ 10 | 0 | ⚠️ 1（假数据） | 9 |
| 积极运动 `exercise` | ✅ 3 | 7 | 0 | 10 |
| 良好睡眠 `sleep` | ✅ 5 | 5 | 0 | 10 |
| 合理视屏 `screen` | ✅ 1 | 9 | 0 | 10 |
| 禁烟禁酒 `habits` | ✅ 6 | 4 | 0 | 10 |
| 心理健康 `mental` | ✅ 5 | 5 | ✅ 2（缺简介） | 8 |
| 环环相扣 `loop` | 0 | 10 | 0 | 10 |
| 计划与行动 `action` | ✅ 4 | 6 | 0 | 10 |

**访客现在实际看到的样子**（线上实测）：

- 营养饮食 → PDF 满 10 份；视频仍是那条示例数据
- 积极运动 / 良好睡眠 / 合理视屏 / 禁烟禁酒 / 心理健康 / 计划与行动 → 都已有 PDF，视频位置空着
- 环环相扣 `loop` → 唯一一个 PDF 和视频都还是空的模块

---

## 四、功能没接完（1 处）

**公众号二维码**

- `content.json` 第 20–24 行预留了 `wechat.qrCode: "assets/images/mp-qrcode.jpg"`
- 但该图片**不存在**，而且**没有任何页面读取这段配置**
- 结果是"扫码关注公众号"这个引流入口目前是空的，访客无处可扫

需要：① 拿到公众号二维码图片 ② 放到 `assets/images/` ③ 再让我在页脚接上显示逻辑

---

## 五、文案与现状不符（需要改文字）

| 位置 | 现在写的 | 实际情况 |
|---|---|---|
| 第 35 行 | "接下来你会看到 **6 个模块**……每个模块都**配了简短的视频和 PDF 手册**" | 网站其实有 8 块内容（6 个模块 + 环环相扣 + 计划与行动）；而且绝大多数模块目前既没有视频也没有 PDF |
| 第 36 行 | "也可以**扫码**关注我们的公众号" | 页面上没有二维码可扫 |

这两句是"承诺了但还没兑现"的文案，等内容补齐后再保留，或者先改措辞。

---

## 六、页面代码里留着的示例数据（访客看不到）

这些是早期做版式时写的**假数据**，只有当 `content.json` 读取失败时才会作为兜底显示出来。线上目前不会出现，但它们在文件里：

| 文件 | 行号 | 内容 |
|---|---|---|
| `pages/health-detail.html` | 512–599 行 | 36 条示例条目，如"三餐搭配实用图表""一周运动打卡表""拒绝烟酒的 10 种说法"，链接全是 `#` |
| `pages/health-loop.html` | 488–497 行 | 6 条示例（"健康行为的连锁反应""21 天养成一个习惯？"等） |
| `pages/mortise.html` | 488–497 行 | 6 条示例（"SMART 原则实操手册""从知道到做到"等） |

> 留着不影响访客。但如果哪天你发现"页面上突然多出一批没见过的卡片"，就是这里起作用了——说明 `content.json` 没加载成功。

---

## 七、素材目录现状

| 模块目录 | `assets/pdfs/<模块>/` | `assets/videos/<模块>/` |
|---|---|---|
| diet | 10 份 ✅（原为 PPTX，已转 PDF） | 空 |
| exercise | 3 份 ✅ | 空 |
| sleep | 5 份 ✅ | 空 |
| screen | 1 份 ✅ | 空 |
| habits | 6 份 ✅ | 空 |
| mental | 4 份 ✅ | `image1.png`、`image2.png` ✅ |
| loop | 空 | 空 |
| action | 4 份 ✅ | 空 |

另有一个闲置文件：`assets/images/微信图片_20260711111423_53_30.jpg`（一张小猪绕毛线球的插画），目前没有任何页面引用它。

### 7.1 这批素材从哪来

2026-09-13 从 `/Users/dd/Documents/srz-intervention-assets/PDF上传/` 上传，对应关系：

| 来源子目录 | 网站模块 | 份数 |
|---|---|---|
| `健康小知识/营养饮食` | `diet` | 10（PPTX 转 PDF） |
| `健康小知识/积极运动` | `exercise` | 3 |
| `健康小知识/良好睡眠` | `sleep` | 5 |
| `健康小知识/合理视屏` | `screen` | 1 |
| `健康小知识/吸烟饮酒` | `habits` | 5 |
| `健康小知识/压力与心理健康` | `mental` | 3 + 解决办法（一）（二） |

> **营养饮食 10 份已于 2026-09-13 更换为在 PowerPoint/WPS 里导出的版本**：
> 第一版是用 LibreOffice 从 PPTX 转的，而 PPTX 用的是「微软雅黑 / 宋体 / 华文新魏 / 汉仪正圆」等
> Windows 字体，本机没有，字体回退导致多页文字错位甚至整页近空白。新版 60 页逐页核对无问题。
| `行动与计划` | `action` | 4 |

原始素材库（218MB，含 PPTX 原件）仍在原处，没有复制进项目。工作表格（视频清单、名单）放在 `_source/`，其中**名单含参与者个人信息，已在 `.gitignore` 中排除，不会上传**。

---

## 八、填写模板（复制即用）

**加一个 PDF**：先把文件放进 `assets/pdfs/<模块>/`，再改对应位置：

```json
{ "id": "diet-pdf-2", "title": "这里写卡片标题", "url": "assets/pdfs/diet/文件名.pdf" }
```

**加一个视频**：封面图放进 `assets/videos/<模块>/`：

```json
{
  "id": "diet-vid-2",
  "title": "视频标题",
  "cover": "assets/videos/diet/封面.png",
  "author": "作者/出处",
  "intro": "一句话简介",
  "url": "https://mp.weixin.qq.com/s/xxxx"
}
```

规则提醒：

- `title` 和 `url` **两个都填**才会显示卡片；任一为空，卡片不显示
- 视频建议用外链（公众号 / B 站），不要把大视频文件传进仓库
- `<模块>` 只能是：`diet` `exercise` `sleep` `screen` `habits` `mental` `loop` `action`

---

## 九、一个提醒

`docs/` 是网站的发布目录，**这个文件夹里的所有文件都会被公开访问**（包括本清单和 `README.md`）。

目前这些文件里没有敏感信息，问题不大。但如果你希望内部笔记不上网，告诉我，我把它们挪到 `docs/` 之外。
